{
  "name": "notes-quotes-and-goats",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "copy": "cp extension/popup.html build/ && cp extension/manifest.json build/ && cp extension/onInstall.js build/ && cp extension/images/goat.png build/",
    "compile": "tsc && npm run copy",
    "lint:fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "Kyle Gibson",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "0.0.127",
    "typescript": "^4.1.3",
    "gts": "^3.0.3",
    "@types/node": "^14.11.2"
  }
}
